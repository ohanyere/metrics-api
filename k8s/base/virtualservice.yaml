apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: metrics-api
  labels:
    app.kubernetes.io/name: metrics-api
spec:
  hosts:
    - metrics-api.dev.platform.ohanyere.internal
  gateways:
    - istio-system/platform-gateway
  http:
    - route:
        - destination:
            host: metrics-api
            port:
              number: 80
          weight: 100
